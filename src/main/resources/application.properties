# 服务器配置
server.port=8089

# 应用名称
spring.application.name=demo

# 日志级别配置
logging.level.root=INFO
logging.level.com.example.demo=DEBUG

# 数据库连接配置
spring.datasource.url=jdbc:mysql://localhost:3306/emojiDB?useSSL=false&serverTimezone=UTC&characterEncoding=utf-8&allowPublicKeyRetrieval=true&autoReconnect=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=rmj20040429
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 添加更多连接参数以提高兼容性
spring.datasource.hikari.connection-test-query=SELECT 1
# （已删除重复项，统一使用下方1800000配置）

# Hikari连接池配置
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false